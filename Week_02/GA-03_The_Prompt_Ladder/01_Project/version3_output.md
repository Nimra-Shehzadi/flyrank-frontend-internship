Same code as Version 2, just split into three files the way beginners are usually taught to structure a project:

index.html — structure only, links to the other two files
style.css — all styling
script.js — all the validation logic

Nothing else changed — same fields, same validation, same comments. This is a good example for your case study of how the output format instruction alone changes the deliverable structure without touching the actual logic or design decisions.